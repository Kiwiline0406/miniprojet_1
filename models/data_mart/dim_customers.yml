version: 2

models:
    - name: dim_customers
      description: The dimension table Customer data with basic cleaning and transformation applied, one row per customer.
      columns:
        - name: customer_id
          description: The unique key for each customer (integer)
        - name: customer_name
          description: The full name of the customer (string).
        - name: customer_status
          description: The rank of each customers based on its orders count (string).